package org.example.studentmanagement.config;

// JWT Filter is disabled for simplified testing
// This class is kept for future use when security is re-enabled
public class JwtAuthenticationFilter {
    // JWT functionality is temporarily disabled
    // This allows the application to run without security constraints
} 